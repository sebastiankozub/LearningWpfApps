<Page x:Class="LearningWpfApp.Views.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LearningWpfApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{Binding Title}"
      DataContext="{Binding StartPageViewModel, Source={StaticResource Locator}}"
      >

    <Grid>
        <Button Content="Open Szama Menu" Command="{Binding OpenMenuCommand}" HorizontalAlignment="Left" Margin="524,117,0,0" VerticalAlignment="Top" Height="108" Width="217" />
    </Grid>
</Page>
